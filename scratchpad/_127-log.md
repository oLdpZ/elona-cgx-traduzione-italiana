
## [2026-09-02] work | Elona ITA — il fronte delle righe nude da 141 a 45, e la gemella l'avevo lasciata io

- **Novantasei righe nude in meno in nove lotti, 78 toppe** (1.088 → **1.166**), e il fronte scende da **141 a 45** — di cui **44 dichiarate in `invariati.md`**, perché `nudi_en` e `triage_nudi` contano i letterali **intatti** e una riga che deve restare intatta è indistinguibile da una che nessuno ha guardato. `command.hsp` 51 → 22, `config.hsp` 9 → 0, `chara.hsp` 16 → 2, `text.hsp` 6 → 0. ⭐ E `_126-sorella-jp` dice ora **0 con sorella**: ogni riga nuda che avesse una fonte giapponese è stata resa, e quel referto ha finito il suo mestiere.
- **⚠️⚠️⚠️ Quattro famiglie erano toppate a metà, e la terza l'avevo lasciata a metà io.** La scala del potenziale (i sette gradini della scheda erano italiani da sessioni, i cinque di `dump_chara` no); le suppliche di chi sta per morire (il giapponese di `ai.hsp:805/813/821` è **identico parola per parola** a `proc.hsp:20596/20677/20756`, resi ieri); «YOU COMMITED TAX EVASION.» in `event.hsp:4560`, gemella esatta di `command.hsp:15556` — **toppata da me la mattina di questa stessa sessione, tre ore prima**. Le prime due si spiegano con la memoria; la terza no. **Non è la memoria a cedere: è l'unità di lavoro** — chi topa guarda il blocco, e il blocco è il perimetro dentro cui una gemella non si vede. La cura costa dieci secondi: `grep -n 'LA STRINGA' *.hsp` su tutto il sorgente prima di chiudere. ⚠️ E si cerca **l'originale**, non l'inglese: per le suppliche i due inglesi erano diversi, il giapponese identico.
- **⚠️⚠️ E la quarta dice la variante peggiore: una toppa può far scadere una resa.** `chat.hsp:14246` spiegava il potenziale dicendo «Supreme è il massimo, Hopeless è il minimo». Era **giusta quando fu scritta**: a schermo la scala era inglese. Poi sette toppe l'hanno tradotta, e da quel giorno la lezione mandava il giocatore a cercare due parole che non esistono più. **Nessuna rete poteva vederlo**: la spiegazione è dizionario, le etichette sono toppe, e nessun referto mette a confronto i due mondi — è la scoperta della 126ª vista dal lato opposto.
- **⚠️⚠️ Cinque toppe scrivevano i puntini con un carattere a due byte.** `…` (U+2026) in CP932 **esiste** — è `0x81 0x63` — quindi la rete che chiede se CP932 sa scrivere il carattere restava verde: chiede che sia **scrivibile**, non che sia **leggibile**. Ma è a doppia larghezza, e il ramo che la build italiana esegue disegna col carattere latino di `config.txt`: i due byte diventano due glifi a caso. Il progetto lo sapeva — `…` è in cima ai `PROIBITI` di `guardie.py` dalla 33ª — ma `guardie.py` legge il **dizionario**, e una toppa una firma non ce l'ha.
- **⭐⭐ La rete nuova guarda i BYTE, non un elenco.** La prima stesura copiava `…“”～«»`, sei caratteri visti sul campo; la versione buona chiede `len(c.encode("cp932")) == 1`. L'elenco avrebbe lasciato passare `—`, `–`, `’`, `※` — e **il trattino lungo stavo per scriverlo davvero**, mezz'ora dopo, traducendo un paragrafo di lore. ⚠️ **La rete è stata esercitata**: rimettendo il `toppe.jsonl` di ieri diventa rossa e nomina la riga, con quello di oggi è verde, più una prova su righe inventate nei due versi. Nata la pagina `un-elenco-enumera-una-misura-chiude`, e **corretta** `cp932-perdite-silenziose`, che consigliava `“ ”` come «equivalente semantico diretto» delle virgolette dritte: è vero per la codifica e **falso per lo schermo**.
- **⚠️⚠️ Una frase cominciava in italiano e finiva in inglese.** `chat.hsp:17065` era la **sesta e ultima** riga di una descrizione spezzata su sei `mes`, di cui le prime cinque passano da `lang()` ed erano italiane da sessioni: a schermo si leggeva «…Uscire senza salva ed esci *option from the ESC menu will result in a penalty at load time*.» **Nessun conteggio poteva distinguerla** — per `nudi_en` era un letterale intatto come gli altri — e a vederla serve leggere il blocco. Il progetto ha `code-virgolette.py` per il caso opposto; per questo non ha niente.
- **⭐⭐ «Master» diceva padrone anche a una giocatrice.** `text.hsp:7051`/`:12081`, il segnaposto `{syujin}`: il giapponese sceglie fra 「ご主人様」 e 「お嬢様」 **guardando il sesso del giocatore**, l'inglese dice «master» in tutt'e due i casi. Qui il sesso **si conosce** — la riga sta dentro un `if ( cdata(CDATA_SEX, CHARA_PLAYER) == 0 )` — ed è l'eccezione che la guida di stile prevede: la regola vieta il participio *quando il genere è ignoto*. Toppa a blocco, con la forma **copiata dal ramo giapponese tre righe sopra**: quando l'inglese collassa due vie che il giapponese tiene distinte, non si progetta, si ricalca.
- **E l'inglese aveva appiattito tre scene su una.** `command.hsp:15042/15053/15064` sono i tre gradini con cui un PNG reagisce al regalo di cioccolato, e in inglese sono **la stessa riga copiata tre volte** (`"I love you." / "Thank you."`) dove il giapponese ha tre scene diverse. È il difetto della 126ª — la provocazione che diceva l'insulto — **nella forma opposta**: non copiato da un'altra scena, ma appiattito su se stesso. ⭐ In `calculation.hsp` invece l'inglese confonde due bestie: ナメクジ è la **lumaca**, かたつむり la **chiocciola**, e «snail» le diceva tutt'e due.

**File toccati**
- Repo `Elona+ CGX - Traduzione Italiana`: `toppe.jsonl` (1.088 → **1.166**), `dizionario/chat.hsp.jsonl` (una resa scaduta), `invariati.md` (tre sezioni nuove), `strumenti/tests/test_toppe.py` (**due prove nuove**, 794 → **796**), nove `scratchpad/_127-*.py` e i loro `.jsonl` in `lavoro/`, `RIPRESA-sessione.md`, `decisioni.md`.
- Fuori dal repo: albero di build ricostruito e `elonaplus2.31/cgx-test.exe` ricompilato (**16:25 del 02/09**). I sei file dati non sono cambiati.
- Vault: `wiki/concepts/un-elenco-enumera-una-misura-chiude.md` (nuovo), `wiki/concepts/cp932-perdite-silenziose.md` (**correzione**), `wiki/concepts/una-famiglia-sparsa-si-scrive-tutta-insieme.md` (sezione nuova), `index.md`, `log.md`.

**Da fare alla prossima sessione**
- **⭐⭐⭐ Il fronte delle righe nude è finito come lotto di lavoro.** Restano 45 righe di cui 44 dichiarate: chi riapre la ripresa e legge «45» non ha davanti un lotto. L'unica cosa aperta e misurata è **gli AP di `chara_func.hsp:8430`/`:8522`**: `gain_ap_source` è **operando e testo insieme** (sette confronti lo leggono per scegliere il ramo) e la frase si compone **per ricorsione** con quattro frammenti inglesi, uno dei quali porta `his()` a un argomento, che è morfologia. La strada — una tabella al sito di stampa, tre basi per cinque code — è scritta in `invariati.md`.
- **Il debito di collaudo, ~9.650, e nessuno strumento che lo misuri.** Nessuna delle 78 toppe di oggi è stata vista a schermo. ⭐ Ma parecchie stanno dove si passa comunque: la schermata del titolo senza salvataggi, il menu delle opzioni, la creazione del personaggio coi geni, il pannello di chi si guarda, il rientro a casa.
- **Due referti che nessuno ha scritto**, e che oggi sarebbero serviti: la **coda nuda di una `lang()` già resa** (avrebbe trovato la frase mozzata senza leggere il blocco), e la **resa che cita per nome un'etichetta che vive in una toppa** (avrebbe trovato la spiegazione del potenziale).
- ⚠️ **Sulle toppe girano tre referti — participi, elisioni, caratteri a doppia larghezza — e nessuno che guardi il glossario, le larghezze o le maiuscole.** Sono 905 toppe con testo dentro.
- ⓘ **Una sola resa della giornata non è riscossa da nessuna parte**: `config.hsp:950`, dove `"English"` diventa **«Italiano»** e non «Inglese», perché in questa build il ramo `en` *è* la traduzione. È segnata in `decisioni.md`: se sembrerà sbagliata, è una toppa sola.
