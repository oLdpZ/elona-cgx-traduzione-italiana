
## [2026-09-02] work | Elona ITA — quattro rami del gioco erano morti, e a ucciderli era stata la traduzione

- **⭐⭐⭐ Il fronte «misurato e non chiuso» che la 127ª lasciava era misurato male, e si è chiuso con due toppe.** Tre documenti — ripresa, `decisioni.md`, `invariati.md` — descrivevano gli AP di `chara_func.hsp` come «tre basi per **cinque code**», una frase composta per ricorsione con un `his()` da togliere, «vuole una sessione sua». Le otto chiamate che compongono quelle code stanno tutte dentro `gain_ap_old`, che ha **un solo chiamante in tutto il sorgente** (`action.hsp:8992`, con `"destone"`), dentro due `if` che confrontano quella stessa variabile con `"talk"` e `"kill"`: **codice irraggiungibile**. ⭐ La prova stava scritta in inglese tre righe sopra la funzione viva: «*Ano made ap gain functions a lot simpler in 2.29, but he didn't change the destone formula*». Chi ha misurato ha letto le righe delle code e **non ha guardato chi chiama la funzione che le contiene**. Una riga si legge nel blocco; un **frammento di frase** si legge nella catena di chiamata.
- **Le tre rese, tutte riscosse e nessuna scelta:** «AP dalla **trattativa**» (`skill.hsp:222` rende `Negotiation` → «Trattativa»; e `"talk"` non è il chiacchierare, è la sconfitta per **persuasione** — `proc.hsp:2338` sfonda gli SP e dà esperienza in `SKILL_NORMAL_NEGOTIATION`), «AP dall'**uccisione**», «AP dalla **pietra del risveglio**». ⚠️ «destone» non era inglese: era `ITEM_ID_AWAKE_DESTONE`, **un identificatore di codice finito dentro una frase** che il giocatore legge. Lì la traduzione non traduce: ripara.
- **⚠️⚠️⚠️ Poi il perimetro chiuso al 100% si è rivelato non voler dire quel che sembrava: quattro `if` del gioco non potevano più essere veri.** Non parole non rese — **rami morti**, perché la traduzione aveva cambiato la stringa **assegnata** e non quella **confrontata**. Ballare nella sala delle feste durava 4 turni invece di 41 (`proc.hsp:1123`); scavare **dentro una nave** dava minerali e polvere di stelle invece di spazzatura, schegge e legname (`:5584`, `:5685`); guardando chi si dichiara di sesso «nessuno», `*txttargetsex` non entrava in nessuno dei sei rami e stampava la parola della frase precedente (`text.hsp:362`).
- **⚠️⚠️ I primi tre erano RINVII CON UNA CONDIZIONE, e la condizione era scaduta da sessioni.** `rinviate.jsonl` diceva, dalla 27ª e dalla 60ª: «va tradotta **INSIEME** a chi assegna il nome della mappa, **non prima**». Chi assegna è stato tradotto in una sessione qualunque — `map_rand.hsp:1287` dice «Sala feste», `map.hsp` dice «Nave mercantile» e «Nave pirata» — e nessuno è tornato a leggere il rinvio. **Un rinvio con una condizione non è una decisione presa: è una decisione rimandata a un evento che nessuno osserva.** La condizione sta in prosa, dentro un campo `motivo`, e nessuno strumento la valuta.
- **⭐⭐ Il quarto è un'altra famiglia ancora: una collisione dentro un file solo.** `lang("なし", "none")` compare due volte in `text.hsp` — a `:49` è la prima voce dell'elenco dei tipi di messaggio, che il giocatore **legge**, e a `:362` è un **operando**. Il dizionario è indicizzato **per contenuto**: una firma sola, e `applica` la sostituisce in tutt'e due. Non è una svista di chi ha reso la voce: è una proprietà dell'indice.
- **✅ Il cancello nuovo, e il numero che conta non è il totale.** `_128-confronti-contro-un-nome-assegnato.py`: per ogni `X == lang(J, E)` cerca un `X = lang(J, A)`; se `A != E` e la riga non porta anche `A`, il ramo è morto. ⭐ **La chiave è il giapponese, non l'inglese** — stessa lezione della 127ª sulle famiglie sparse. Dice **3 rami morti di monte** (`command.hsp:3639` confronta «bisexual» dove tutti assegnano «hermaphrodite»; `text.hsp:359` ha il refuso «hermaph*or*odite»; `init.hsp:1981` accoppia 自称男性 con «female?»), che sono rotti anche nel gioco inglese e non si toccano, e **0 aggiunti dalla traduzione**, che è il cancello. ⭐⭐ La prova al contrario è **esercitata**: ricostruisce in una cartella di lavoro l'albero *di ieri* — la build senza le quattro toppe — e pretende che il cancello si accenda nominandole tutte e quattro.
- **La forma della riparazione era già nel progetto, e non si è inventata.** Tutte e quattro le toppe **allargano la condizione** invece di tradurre l'operando: è la migrazione di «Your Home» della 42ª — la `lang()` non si tocca, resta rinviata ed è quel che rende stabile la stringa cercata, e il letterale si aggiunge **fuori** da `lang()` con un `|`. Così il confronto accetta sia il valore nuovo sia quello che un salvataggio vecchio porta scritto dentro (`mdatan` è serializzato).
- **⚠️⚠️ Due invarianti del progetto hanno preso due miei errori prima che uscisse un eseguibile sbagliato.** (1) La sezione nuova di `invariati.md` conteneva una tabella, e `verifica.py` pretende che una sezione con valori sia classificata: `pytest` è passato a **16 rossi** — e li ho visti solo perché la lezione della 112ª dice di rilanciare i test **dopo** aver scritto i documenti. (2) La toppa di `text.hsp:362` l'avevo scritta cercando la riga della **build** («Nessuna»), e `test_le_toppe_del_progetto_si_applicano_al_sorgente_pinnato` è diventato rosso: una toppa deve trovare la sua riga nel **sorgente pinnato**, e le due cose coincidono solo finché il dizionario non tocca quella riga. La versione giusta è dichiarata `prima`.

**File toccati**
- Repo `Elona+ CGX - Traduzione Italiana`: `toppe.jsonl` (1.166 → **1.172**), `rinviate.jsonl` (tre motivi riscritti, la condizione era maturata), `invariati.md`, `decisioni.md` (due sezioni nuove), `RIPRESA-sessione.md` (testa nuova, la 127ª in storia), otto `scratchpad/_128-*.py` e i loro `.jsonl` in `lavoro/`.
- Fuori dal repo: albero di build ricostruito e `elonaplus2.31/cgx-test.exe` ricompilato (**18:10 del 02/09**). I sei file dati non sono cambiati.
- Vault: `wiki/concepts/un-rinvio-con-una-condizione-nessuno-lo-rilegge.md` (nuovo), `wiki/concepts/il-blocco-non-basta-serve-la-catena-di-chiamata.md` (nuovo), `wiki/concepts/coerenza-fra-due-file-uno-solo-tracciato.md` (sezione nuova), `index.md`, `log.md`.

**Numeri, rilanciati dopo l'ultima modifica ai documenti**
- `pytest` 796 passed / 6 skipped; `prova_identita` 72/72 e 30.905; `applica` 30.764 sostituzioni; toppe **1.172 agganciate su 1.172**; perimetro `lang()` 26.326 su 26.326 = 100%; righe nude vive **43** (erano 45); `_128-confronti…` **di monte 3, aggiunti 0**.

**Da fare alla prossima sessione**
- **⭐⭐⭐ Quanti altri rinvii hanno una condizione che nessuno valuta?** `rinviate.jsonl` ha **114 voci**, tre erano scadute, e l'unico modo che ho avuto per cercarle è un `grep` su prosa (`INSIEME|non prima|ASSEGNA`). Ne resta **una quarta** della stessa forma (`item_func.hsp:1386`, il materiale del kit), ancora valida. Un campo strutturato al posto della frase — o un referto che rilegga i rinvii — è il lavoro che manca.
- **Il debito di collaudo, ~9.650, e nessuno strumento che lo misuri.** ⚠️ Nessuna delle sei toppe di oggi è stata vista a schermo, e quattro di esse sono **riparazioni di gioco**, non di testo: il collaudo qui non guarda una parola, guarda che il ballo duri 41 turni e che la nave frutti spazzatura.
- ⚠️ **Sulle toppe girano tre referti — participi, elisioni, caratteri a doppia larghezza — e nessuno che guardi il glossario, le larghezze o le maiuscole.** Sono 911 toppe con testo dentro.
- **Due referti che nessuno ha scritto**: la **coda nuda di una `lang()` già resa** e la **resa che cita per nome un'etichetta che vive in una toppa**. E uno nato oggi: nessuno guarda se un frammento inglese nudo stia in **codice irraggiungibile per flusso** — non `if ( FALSE )`, ma «la funzione che lo contiene non è mai chiamata con quel valore».
