# -*- coding: utf-8 -*-
"""Le rese del lotto 074 — GLI ALTARI: `FILTER_FURNITURE_ALTAR` si chiude.

    PYTHONIOENCODING=utf-8 python scratchpad/lotti-109/_monta.py 074 scratchpad/lotti-113

Il ragionamento sta per esteso in `testa074.py`.

⚠️⚠️ Forma, da `_forma.py 074`: **2 su 2** con lo spazio prima del `\\n`,
**0 su 2** con lo spazio dopo il `#`.
⚠️⚠️⚠️ **E LO STESSO TITOLO, NELLO STESSO GIORNO, HA LE DUE SPAZIATURE.** Il
lotto 073 ha reso `:90707` con `# ~I Mondi che Non Hai Mai Visto~` (con lo
spazio), perche' li' l'inglese scrive `# ~Worlds…`; qui l'inglese scrive
`#~ Worlds…` e la coda va **senza**. E' il motivo per cui i titoli si copiano
da `_code.py` e non si scrivono a memoria.

⚠️⚠️ Previsione di `applica`: **+2** per 2 rese, nessuna gemella.
ⓘ `_gia-reso 074`: 0 su 2. `_119-togli-rinviate 074`: nessuna rinviata.

⭐⭐ `_122-sorelle-per-frase 074`: **1.00 fra le due righe**. La seconda frase e'
identica parola per parola — ここで祈ることにより神と交感できると言われている
— e le due rese la tengono identica.
"""

IT = {
    # =====================================================================
    # LA FRASE CHE LE DUE RIGHE CONDIVIDONO, TENUTA IDENTICA
    # =====================================================================
    # ⓘ 神 e' SINGOLARE in giapponese, e l'inglese lo mette al plurale («the
    #   Gods»). Vince il giapponese, e l'indice 3 dell'altare dice gia'
    #   «in lode del dio». 交感 -> «entrare in comunione», che e' la parola
    #   italiana per il rapporto col divino.
    # ⓘ 捧げ物 -> «offerte» (dizionario, quattro voci).

    119819: "Un altare spoglio, con giusto il minimo che serve per fare offerte. Dicono che pregando qui si entri in comunione col dio. \\n#~I Mondi che Non Hai Mai Visto~",

    # 醸し出す e' «spandere intorno», detto di un'atmosfera. 厳か -> «solenne».
    119881: "Una costruzione enorme fatta di pietra, che spande intorno un'aria solenne. Dicono che pregando qui si entri in comunione col dio. \\n#~I Mondi che Non Hai Mai Visto~",
}
